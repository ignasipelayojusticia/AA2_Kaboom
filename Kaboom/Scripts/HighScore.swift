//
//  HighScore.swift
//  Kaboom
//
//  Created by Alumne on 8/6/21.
//

import Foundation

/*
let HIGHSCORE_KEY: String = "com.ignasi.kaboom.highscoresKey"

struct FinalScore: Codable {
    let difficulty: Int
    let score: Int
}

let leadBoard: [FinalScore] = [
    FinalScore(difficulty: 1, score: 1830),
    FinalScore(difficulty: 3, score: 1406),
    FinalScore(difficulty: 2, score: 683)
]

do {
    let highScoresData = try JSONEncoder().encode(leadBoard)
} catch {
    print(error)
}

var highScores = UserDefaults.standard.value(forKey: HIGHSCORE_KEY)
UserDefaults.standard.setValue(leadBoard, forKey: HIGHSCORE_KEY)
let data = UserDefaults.standard.value(forKey: HIGHSCORE_KEY) as! Data
JSONDecoder().decode([FinalScore].self, from: data)
*/
